# PyPlanAnalysis
Python module to analyse dose and LETd distributions and extract dose/LETd parameters. It allows computing variable RBE, extracting DVH, LVH, DLVH parameters. It also allows computing relevant NTCP for brain treatments.
